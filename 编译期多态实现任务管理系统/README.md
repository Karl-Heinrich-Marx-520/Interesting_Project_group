# C++ 命令行任务管理系统
一个基于 **C++11** 开发的轻量级命令行任务管理工具，集成任务增删改查、数据持久化、日志记录功能，采用**单例模式**、**CRTP 静态多态**设计，高性能且易于扩展。

## ✨ 核心特性
- 📝 **完整任务操作**：支持添加、删除、更新、查看任务
- 🏷️ **任务属性**：包含描述、优先级（1-高/2-中/3-低）、截止日期（YYYY-MM-DD）
- 📂 **数据持久化**：任务自动保存到本地文件，重启程序不丢失
- 📜 **日志系统**：单例模式实现全局日志记录，操作可追溯
- 🚀 **高性能设计**：CRTP 奇异递归模板模式实现命令接口，无运行时多态开销
- 🎯 **灵活排序**：支持按 ID / 优先级 / 截止日期排序查看任务
- 🛠️ **易扩展**：模块化设计，新增命令无需修改核心逻辑

## 🎯 设计模式
1. **单例模式 (Singleton)**：日志系统全局唯一，线程安全
2. **CRTP (奇异递归模板模式)**：静态多态实现命令接口，提升性能
3. **命令模式 (Command)**：解耦命令调用与执行逻辑
4. **面向对象封装**：任务管理、日志、命令、数据存储完全解耦

## 📁 项目结构
```
TaskManager/
├── main.cpp                # 主程序：处理用户输入、命令调度
├── Logger.h/Logger.cpp     # 单例日志系统
├── Task.h                  # 任务结构体定义
├── TaskManager.h/TaskManager.cpp # 任务核心管理逻辑
├── Command.h               # CRTP 命令接口 + 具体命令实现
├── tasks.txt               # 任务数据持久化文件（自动生成）
├── log.txt                 # 操作日志文件（自动生成）
└── README.md               # 项目说明文档
```

## 🧰 环境要求
- 编译器：支持 **C++11 及以上** (GCC / Clang / MSVC)
- 操作系统：Windows / macOS / Linux
- 无第三方依赖，纯标准库实现

## 🚀 编译与运行
### 1. 编译代码
将所有源码文件放在同一目录下，执行编译命令：
```bash
# GCC/Clang 编译
g++ -std=c++11 main.cpp Logger.cpp TaskManager.cpp -o TaskManager
```

### 2. 运行程序
```bash
# Linux/macOS
./TaskManager

# Windows (CMD/PowerShell)
TaskManager.exe
```

## 📖 使用指南
程序启动后，输入以下命令操作任务：

| 命令 | 用法格式 | 功能说明 |
|------|----------|----------|
| `add` | `add <描述>,<优先级>,<截止日期>` | 添加新任务 |
| `delete` | `delete <任务ID>` | 删除指定 ID 的任务 |
| `update` | `update <ID>,<描述>,<优先级>,<截止日期>` | 更新指定任务 |
| `list` | `list [排序方式]` | 查看任务（0-ID/1-优先级/2-日期） |
| `exit` | `exit` | 退出程序 |

### 使用示例
```bash
> add 完成项目报告,1,2025-05-01
任务添加成功。

> add 购买日用品,2,2025-04-20
任务添加成功。

> list 1  # 按优先级排序
ID: 1, 描述: 完成项目报告, 优先级: 1, 截止日期: 2025-05-01
ID: 2, 描述: 购买日用品, 优先级: 2, 截止日期: 2025-04-20

> update 2,购买食材,1,2025-04-21
任务更新成功。

> delete 1
任务删除成功。

> exit
退出程序。
```

## 📄 文件说明
1. **`tasks.txt`**
   自动生成的任务数据文件，格式：
   ```
   1,完成项目报告,1,2025-05-01
   2,购买食材,1,2025-04-21
   ```
2. **`log.txt`**
   操作日志文件，记录所有任务修改行为 + 时间戳：
   ```
   Wed Apr 10 15:30:00 2025: 添加任务: ID: 1, 描述: 完成项目报告...
   Wed Apr 10 15:31:20 2025: 保存任务成功。
   ```

## 🔍 核心设计详解
### 1. 单例模式 - 日志系统
- 保证全局唯一日志实例，避免多文件同时写入冲突
- 线程安全（`std::mutex` 互斥锁）
- 自动追加写入日志，程序退出自动关闭文件

### 2. CRTP 模式 - 命令接口
**静态多态 vs 动态多态（虚函数）**
| 特性 | CRTP (本项目) | 虚函数继承 |
|------|---------------|------------|
| 多态类型 | 编译期静态多态 | 运行期动态多态 |
| 性能开销 | 零开销，支持内联 | 虚表指针 + 寻址开销 |
| 适用场景 | 编译期确定命令类型 | 运行时动态加载命令 |
| 代码复杂度 | 模板语法，编译期检查 | 语法简单，直观 |

本项目使用 CRTP 追求**极致性能**，适合命令类型固定的命令行工具。

## 🛠️ 扩展指南
如需新增命令（如 `clear` 清空任务）：
1. 在 `Command.h` 中继承 `Command<T>` 实现新命令类
2. 实现 `executeImpl` 方法
3. 在 `main.cpp` 中注册命令到映射表
4. 无需修改其他模块代码

## 🤝 贡献规范
1. Fork 本仓库
2. 创建功能分支 (`feature/xxx` 或 `fix/xxx`)
3. 提交代码并编写清晰的 commit 信息
4. 提交 Pull Request

## 📜 许可证
本项目基于 **MIT License** 开源，可自由使用、修改、分发。

---
**提示**：首次运行会自动生成 `tasks.txt` 和 `log.txt` 文件，无需手动创建~
